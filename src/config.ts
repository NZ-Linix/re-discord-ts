export default {

    guildId: "", // If empty, commands will be registered globally, otherwise only to the guild
    messageCommandPrefix: ">", // Prefix for message commands

    status: {
        rotations: [], // Array of presences to rotate through
    }

}